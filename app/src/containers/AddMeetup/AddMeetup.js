import React, { Component } from 'react';
import styles from './AddMeetup.bundle.scss';
import CSSModules from 'react-css-modules';

class AddMeetup extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

export default CSSModules(AddMeetup, styles);
