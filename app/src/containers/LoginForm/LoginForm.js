import React, { Component } from 'react';
import styles from './LoginForm.bundle.scss';
import CSSModules from 'react-css-modules';

class LoginForm extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

export default CSSModules(LoginForm, styles);
